import { Meta } from "@storybook/blocks";
import { getVersion } from './utils/version.js';

<Meta title="Documents" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Strato StoryBook
    <br/>
    ## 📦 Current Version: {getVersion()}
    <br/>
    ## 📦 Nexus Initialize(`.npmrc`)
    ```bash
    registry=https://registry.npmjs.org/
    @strato:registry=http://210.217.178.150:8081/repository/strato-ui/
    ```

    ## 📦 Install Library
    ```bash
    npm install @strato/components
    yarn add @strato/components
    ```

    ## 📌 Initialize(`main.js`)
    ```javascript
    import { createApp } from 'vue';
    import { createVuetify } from 'vuetify';
    import * as components from 'vuetify/components';
    import * as directives from 'vuetify/directives';
    import { customTheme } from '@strato/components';

    // styles
    import '@mdi/font/css/materialdesignicons.css';
    import '@strato/components/lib/style.css'; // Vuetify 스타일 포함

    const Vuetify = createVuetify({components, directives, theme: { ...customTheme }});
    createApp(App).use(Vuetify).mount('#app')
    ```
  </div>
</div>
<style>
  {`
  .sb-container {
    margin-bottom: 48px;
  }
  `}
</style>
